{
  "name": "mockingboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/shared",
    "packages/bot",
    "packages/web"
  ],
  "scripts": {
    "dev:bot": "npm run dev -w packages/bot",
    "dev:web": "npm run dev -w packages/web",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build": "npm run build --workspaces --if-present",
    "verify": "npm run lint && npm run test && npm run test:e2e && npm run format:check",
    "clean_artifacts": "rm -rf packages/**/dist packages/**/node_modules",
    "clean_code": "eslint . --fix && prettier --write .",
    "clean_install": "npm run clean_artifacts && npm install",
    "register_commands": "npm run deploy-commands -w packages/bot",
    "seed:prospects": "npm run seed:prospects -w packages/bot",
    "seed:scouts": "npm run seed:scouts -w packages/bot",
    "lighthouse": "lhci autorun",
    "analyze": "ANALYZE=true npm run build -w packages/web"
  },
  "devDependencies": {
    "@eslint/js": "^9.18.0",
    "@lhci/cli": "^0.15.1",
    "@playwright/test": "^1.58.2",
    "eslint": "^9.18.0",
    "eslint-config-prettier": "^10.0.0",
    "prettier": "^3.4.0",
    "turbo": "^2.8.1",
    "typescript": "~5.7.0",
    "typescript-eslint": "^8.20.0",
    "vitest": "^4.0.18"
  },
  "packageManager": "npm@11.5.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "lightningcss": "^1.31.1"
  }
}
